# Gate 2 — DS-alignment warning

The design source isn't from a canonical project. Translation cost will be higher than usual — expect more raw-hex fixes, more component substitutions, and a longer mapping phase.

Specifics:

{{warning_detail — e.g.:}}
- *Claude Design export from a non-Open-Session team project (no BOS DS pre-loaded).*
- *Figma file not linked to your Untitled UI v8 library.*
- *V0 export uses Radix Tabs; your DS uses React Aria — every Tab will need substituting.*

This is **warn-only** — I'm not blocking. But mention this to your reviewer; mid-slice surprises are more likely.

Continuing to Gate 3 (target audit).
